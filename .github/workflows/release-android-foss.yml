name: Release Android (FOSS)

on:
  workflow_call:
    inputs:
      tag:
        description: 'Release tag (e.g., v0.2.3)'
        required: false
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v0.2.3)'
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: release-android-foss-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android-foss:
    runs-on: ubuntu-latest
    name: Android FOSS Build
    timeout-minutes: 90
    env:
      # Keep Metro/JS bundling stable on CI during release builds.
      NODE_OPTIONS: --max-old-space-size=6144
      DROPBOX_APP_KEY: ${{ vars.DROPBOX_APP_KEY || secrets.DROPBOX_APP_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install pinned Node.js (match reproducible recipe)
        run: |
          wget -q -O /tmp/nodejs-lts.tar.xz -- https://nodejs.org/dist/v24.13.1/node-v24.13.1-linux-x64.tar.xz
          sha256sum -c <<< '30215f90ea3cd04dfbc06e762c021393fa173a1d392974298bbc871a8e461089  /tmp/nodejs-lts.tar.xz'
          tar xf /tmp/nodejs-lts.tar.xz -C /opt
          echo "/opt/node-v24.13.1-linux-x64/bin" >> "$GITHUB_PATH"
          /opt/node-v24.13.1-linux-x64/bin/node -v
          /opt/node-v24.13.1-linux-x64/bin/npm -v

      - name: Setup Java
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3

      - name: Prepare FOSS dependency manifest
        run: bash apps/mobile/scripts/fdroid_prep.sh

      - name: Install core dependencies
        run: npm ci --workspaces=false --legacy-peer-deps --no-audit --no-fund --prefix packages/core

      - name: Install mobile dependencies
        run: npm ci --workspaces=false --legacy-peer-deps --no-audit --no-fund --prefix apps/mobile

      - name: Resolve version
        id: version
        run: |
          INPUT_TAG="${{ inputs.tag || github.event.inputs.tag }}"
          GITHUB_EVENT_INPUTS_TAG="${{ github.event.inputs.tag }}"
          scripts/ci/resolve-release-version.sh "$INPUT_TAG" >> "$GITHUB_OUTPUT"

      - name: Build FOSS Android APK (Gradle, arm64-v8a only)
        timeout-minutes: 30
        run: |
          UNSIGNED_APK=1 FOSS_BUILD=1 SKIP_FDROID_PREP=1 ARCHS=arm64-v8a bash apps/mobile/scripts/android_build.sh
          ls -la apps/mobile/build

      - name: Sign FOSS APK with release key
        timeout-minutes: 15
        env:
          ANDROID_RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
          ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          ANDROID_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
          ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          for required in \
            ANDROID_RELEASE_KEYSTORE_BASE64 \
            ANDROID_RELEASE_KEY_ALIAS \
            ANDROID_RELEASE_STORE_PASSWORD \
            ANDROID_RELEASE_KEY_PASSWORD; do
            if [[ -z "${!required:-}" ]]; then
              echo "::error::Missing required secret: ${required}" >&2
              exit 1
            fi
          done

          mapfile -t apk_matches < <(find apps/mobile/build -maxdepth 1 -type f -name 'mindwtr-*-arm64-v8a-foss.apk' | sort)
          if [[ "${#apk_matches[@]}" -ne 1 ]]; then
            echo "Expected exactly one arm64 FOSS APK for signing, found ${#apk_matches[@]}" >&2
            printf '%s\n' "${apk_matches[@]}" >&2
            exit 1
          fi
          APK_PATH="${apk_matches[0]}"

          APKSIGNER_BIN="$(command -v apksigner || true)"
          ZIPALIGN_BIN="$(command -v zipalign || true)"
          if [[ -z "${APKSIGNER_BIN}" ]]; then
            APKSIGNER_BIN="$(ls "${ANDROID_HOME}"/build-tools/*/apksigner 2>/dev/null | sort -V | tail -1)"
          fi
          if [[ -z "${ZIPALIGN_BIN}" ]]; then
            ZIPALIGN_BIN="$(ls "${ANDROID_HOME}"/build-tools/*/zipalign 2>/dev/null | sort -V | tail -1)"
          fi
          if [[ -z "${APKSIGNER_BIN}" || -z "${ZIPALIGN_BIN}" ]]; then
            echo "apksigner/zipalign not found in Android SDK build-tools" >&2
            exit 1
          fi

          KEYSTORE_PATH="${RUNNER_TEMP}/android-release.keystore"
          printf '%s' "${ANDROID_RELEASE_KEYSTORE_BASE64}" | base64 --decode > "${KEYSTORE_PATH}"
          chmod 600 "${KEYSTORE_PATH}"

          STORE_PASS_FILE="${RUNNER_TEMP}/android-store-pass.txt"
          KEY_PASS_FILE="${RUNNER_TEMP}/android-key-pass.txt"
          printf '%s' "${ANDROID_RELEASE_STORE_PASSWORD}" > "${STORE_PASS_FILE}"
          printf '%s' "${ANDROID_RELEASE_KEY_PASSWORD}" > "${KEY_PASS_FILE}"
          chmod 600 "${STORE_PASS_FILE}" "${KEY_PASS_FILE}"

          UNSIGNED_APK="${RUNNER_TEMP}/mindwtr-foss-unsigned.apk"
          ALIGNED_APK="${RUNNER_TEMP}/mindwtr-foss-aligned.apk"
          SIGNED_APK="${RUNNER_TEMP}/mindwtr-foss-signed.apk"

          cp "${APK_PATH}" "${UNSIGNED_APK}"
          "${ZIPALIGN_BIN}" -f -p 4 "${UNSIGNED_APK}" "${ALIGNED_APK}"

          "${APKSIGNER_BIN}" sign \
            --ks "${KEYSTORE_PATH}" \
            --ks-key-alias "${ANDROID_RELEASE_KEY_ALIAS}" \
            --ks-pass "file:${STORE_PASS_FILE}" \
            --key-pass "file:${KEY_PASS_FILE}" \
            --out "${SIGNED_APK}" \
            "${ALIGNED_APK}"

          mv "${SIGNED_APK}" "${APK_PATH}"
          rm -f "${STORE_PASS_FILE}" "${KEY_PASS_FILE}" "${KEYSTORE_PATH}"

          SIG_REPORT="${RUNNER_TEMP}/mindwtr-foss-signature.txt"
          "${APKSIGNER_BIN}" verify --verbose --print-certs "${APK_PATH}" | tee "${SIG_REPORT}"
          if grep -q "CN=Android Debug" "${SIG_REPORT}"; then
            echo "Debug certificate detected in final APK signer chain" >&2
            exit 1
          fi
          echo "FOSS APK signed with release key."

      - name: Verify FOSS permission cleanup
        run: |
          mapfile -t apk_matches < <(find apps/mobile/build -maxdepth 1 -type f -name 'mindwtr-*-arm64-v8a-foss.apk' | sort)
          if [[ "${#apk_matches[@]}" -ne 1 ]]; then
            echo "Expected exactly one arm64 FOSS APK for permission verification, found ${#apk_matches[@]}" >&2
            printf '%s\n' "${apk_matches[@]}" >&2
            exit 1
          fi
          APK_PATH="${apk_matches[0]}"
          AAPT_BIN="$(command -v aapt || true)"
          if [[ -z "${AAPT_BIN}" ]]; then
            AAPT_BIN="$(ls "${ANDROID_HOME}"/build-tools/*/aapt 2>/dev/null | sort -V | tail -1)"
          fi
          if [[ -z "${AAPT_BIN}" ]]; then
            echo "aapt not found; cannot verify APK permissions" >&2
            exit 1
          fi
          PERMS_DUMP="$(mktemp)"
          "${AAPT_BIN}" dump permissions "${APK_PATH}" | tee "${PERMS_DUMP}"
          for perm in \
            android.permission.CAMERA \
            android.permission.SYSTEM_ALERT_WINDOW \
            android.permission.READ_EXTERNAL_STORAGE \
            android.permission.WRITE_EXTERNAL_STORAGE; do
            if grep -q "name='${perm}'" "${PERMS_DUMP}"; then
              echo "${perm} still present in APK manifest" >&2
              exit 1
            fi
          done
          if ! grep -q "name='android.permission.RECORD_AUDIO'" "${PERMS_DUMP}"; then
            echo "android.permission.RECORD_AUDIO missing in APK manifest" >&2
            exit 1
          fi
          echo "Verified APK permissions: CAMERA/storage/alert removed; RECORD_AUDIO present."

      - name: Collect Android FOSS artifacts
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mapfile -t apk_matches < <(find apps/mobile/build -maxdepth 1 -type f -name 'mindwtr-*-arm64-v8a-foss.apk' | sort)
          if [[ "${#apk_matches[@]}" -ne 1 ]]; then
            echo "Expected exactly one arm64 FOSS APK for collection, found ${#apk_matches[@]}" >&2
            printf '%s\n' "${apk_matches[@]}" >&2
            exit 1
          fi
          APK_PATH="${apk_matches[0]}"
          mkdir -p release-artifacts
          cp "${APK_PATH}" "release-artifacts/mindwtr-${VERSION}-foss.apk"
          ls -la release-artifacts

      - name: Upload Android FOSS artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: release-android-foss
          path: release-artifacts/*
          if-no-files-found: error
          retention-days: 28
